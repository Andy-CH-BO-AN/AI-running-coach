#
# Thin Codex adapter. Canonical metadata is maintained in:
# ai/skills/python-review-qa-loop/agents/openai.yaml
#
interface:
  display_name: "Python Review QA Loop"
  short_description: "Run the repo's Python change, review, QA, and security loop"
  default_prompt: "Use $python-review-qa-loop to follow the repo's shared Python review, QA, and security workflow while making or validating code changes."
